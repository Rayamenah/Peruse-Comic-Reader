/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.asurascans.com", "asura.gg", "img.freepik.com", "flamescans.org"],
  },
};

module.exports = nextConfig;
